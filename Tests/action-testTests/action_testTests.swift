import XCTest
@testable import action_test

final class action_testTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(action_test().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
