/// A type for things so I can test this...is embarrasing...it would be nice to be able to run this locally?
struct action_test {
    /// A property to contain hello world text!!!
    var text = "Hello, World!"
}
